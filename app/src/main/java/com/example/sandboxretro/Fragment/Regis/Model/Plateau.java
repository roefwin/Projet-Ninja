package com.example.sandboxretro.Fragment.Regis.Model;

import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;

import java.util.List;

public class Plateau {

    /*private int taille;
    private Paint pLignes;
    private List<Boat> historique;

    public Plateau(Context context, int taille, List<Boat> historique) {
        super(context);

        this.taille = taille;
        this.historique = historique;

        pLignes = new Paint();
        pLignes.setColor(Color.BLACK);
        // ...
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
        int w = getWidth();
        int h = getHeight();
        int rayon = Math.min(w, h) / taille / 2;

        // Quadrillage
        for (int i = 1; i < taille; i++) {
            canvas.drawLine(i * w / taille, 0, i * w / taille, h, pLignes);
        }

        for (int j = 1; j <= taille; j++) {
            canvas.drawLine(0, j * h / taille, w, j * h / taille, pLignes);
        }
    }*/

}
